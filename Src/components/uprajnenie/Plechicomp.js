import React from 'react';
import { SafeAreaView, Text } from "react-native";




export const PlechiComp = ({navigation}) => {
        return (                   
        <SafeAreaView>
            <Text>Плечи</Text>                  
        </SafeAreaView>     
    );
};
